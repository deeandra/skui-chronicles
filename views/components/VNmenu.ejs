<section class="section-general">
    <div class="back">
        <input class="back-menu" type="submit" value="Back" id="back"></input>  
    </div>
        <div class="container-vn-kanan">
            <div class="wrapper">
                <div class="top-symbol border">
                    <img src="/public/images/border.png" alt="border-simbol"></img>
                </div>
                <div class="button1">
                    <input class="menu-button" id="flashcards" type="submit" value="Flashcards"></input>
                </div>
                <div class="button2">
                    <input class="menu-button" id="status-monitor" type="submit" value="Status Monitor"></input>
                </div>
                <div class="button4">
                    <input class="menu-button" id="mission" type="submit" value="Dossier"></input>
                </div>
                <div class="button3">
                    <input class="menu-button" id="save-quit" type="submit" value="Save & Quit"></input>
                </div>
                
                <div class="down-symbol border">
                    <img src="/public/images/border.png" alt="border-simbol"></img>
                </div>
            </div>
        </div>
</section>

<script>
    $(document).ready(function() {
        function resizeFontMenu(){
            let backButtonWidth = $(".back-menu").width();
            let backFontSize = backButtonWidth/5.5;
            if(backFontSize>20){
                backFontSize = 20;
            }
            $(".back-menu").css("font-size", `${backFontSize}px`);
        }
        resizeFontMenu();
        $(window).resize(resizeFontMenu);
    });
</script>